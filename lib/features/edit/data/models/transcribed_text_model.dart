/// Data model for transcribed text
/// TODO: Need to be changed with the actual data model
class TranscribedTextModel {
  final String author;
  final String text;
  

  TranscribedTextModel({
    required this.author,
    required this.text,
  });
}
